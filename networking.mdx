---
title: "Networking (Basic)"
description: "FreeBSD shines in networking but this page is about basic networking for someone like me who's new to the the world of FreeBSD"
---

## To switch between WiFi and Ethernet

When WiFi is default and you want Ethernet

```shellscript
route -n get default # to show default
route delete default # delete default
route add default 192.168.1.1 -ifp re0 # set new default
```