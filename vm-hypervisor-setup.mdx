---
title: "VMHypervisorSetup"
---

```shellscript
sudo apt update
sudo apt install -y \
  qemu-kvm \
  libvirt-daemon-system \
  libvirt-clients \
  virt-manager \
  bridge-utils

sudo systemctl enable --now libvirtd

sudo usermod -aG libvirt,kvm sohamthakurdesai

logout
```